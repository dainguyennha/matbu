<%= form_for :card_product, url: card_products_path(), remote: true,html:{ id:"add-card-f"} do |f| %>
  <%= f.hidden_field :count, value: 1, id:"product-count" %>
  <%= f.hidden_field :product_id, value: product.id %>
  <%= f.hidden_field :size, value: sizes[0].name, id:"product-size-cart" %>
  <div class="add-card"><%= f.submit "Thêm vào giỏ", class: "btn-primary btn-over" %> </div>
<%end%>

